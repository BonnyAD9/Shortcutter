{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "program": "${workspaceFolder}/bin/debug/Shortcutter.exe",
      "args": [],
      "cwd": "${workspaceFolder}/bin/debug",
      "preLaunchTask": "Makefile"
    }
  ]
}
